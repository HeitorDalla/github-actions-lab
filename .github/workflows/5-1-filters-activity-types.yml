name: 5 - 1 - Event Filters and Activity Types

on:
  #  pull_request:
  #    types: [opened, synchronize]
  #    branches:
  #      - main

  workflow_dispatch:

jobs:
  echo:
    name: Job para imprimir mensagem
    runs-on: ubuntu-latest

    steps:
      - name: Step para imprimir mensagem
        run: echo "Running whenever a PR is opened or synchronized AND base branch is main"
